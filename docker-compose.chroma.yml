services:
  chroma:
    image: chromadb/chroma:1.1.0
    restart: always
    ports:
      # 127.0.0.1 only. Chroma runs without authentication; on every interface
      # anyone on the same network could read or overwrite the embeddings.
      - "127.0.0.1:${CHROMA_PORT:-8000}:8000"
    volumes:
      - chroma-data:/chroma/chroma
    environment:
      - "ANONYMIZED_TELEMETRY=false"
      - "IS_PERSISTENT=TRUE"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v2/heartbeat"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  chroma-data:
