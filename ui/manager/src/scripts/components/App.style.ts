import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  body: {
    overflow: 'scroll',
  },
  eddiLogo: {
    display: 'block',
    marginLeft: 'auto',
    marginRight: 'auto',
    width: '50px',
  },
});

export default useStyles;
