import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  body: {
    overflow: 'scroll',
  },
  eddiLogo: {
    display: 'block',
    marginTop: '20px',
    marginBottom: '20px',
    width: '100px',
  },
});

export default useStyles;
