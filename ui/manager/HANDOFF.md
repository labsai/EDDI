# EDDI Manager Project Handoff

## Current Status (v6.3.0)

### Completed Phases
- **Eleven tests could not fail, and one of them was hiding a broken mock** (branch: `claude/code-review-testing-audit-250519`): a repo-wide review measured the suite by mutation rather than by coverage — **20 semantic mutations: 12 killed, 8 survived**, of which one is an equivalent mutant, so 7 are real gaps. The logic layer held (9 killed of 11 non-equivalent, from 12 mutants); the UI layer did not (3 killed of 8, so 5 survived — including "the config editor never registers an edit", which 36 tests pass straight through). The 12th logic mutant is the omitted one: dropping `204` from `if (response.status === 202 || response.status === 204)` in `api-client.ts` changes no behaviour, because a spec-compliant 204 has an empty body and falls through to `if (!text) return undefined` regardless — it is excluded from the denominator rather than counted as a miss. (12 + 8 = 20; 9 + 3 = 12 killed.) This first pass removes the escape hatches that let a red result report green. **5481 passing (353 files)**, count unchanged — no tests added, ten assertions made falsifiable.
  - **Two E2E tests had no assertion in either branch.** The entire body of `admin-pages.spec.ts` "shows variables from MSW mock data" and `resource-editor.spec.ts` "shows API call entries from mock data" was a `test.skip(!visible, "MSW browser worker too slow")` guard: data present meant an empty body, data absent meant a skip. Both are now assertions with a 15s timeout — slow mock data is the bug the test exists to catch, not a reason to opt out.
  - **Nine conditional skips removed.** `resource-editor.spec.ts` treated a crashed Monaco as a pass; it now asserts the error boundary did *not* replace the editor, so the failure says why. The three `if (!pkgId) { test.skip(); return; }` guards in `workflows-crud.fullstack.spec.ts` existed because the fixture package was created inside the *first test* — moved to `beforeAll` with a named expectation, so a broken create fails the describe at setup instead of silently skipping the tests that would show its blast radius. `agent-lifecycle.fullstack.spec.ts` now creates the conversation it renders rather than reading back whatever the previous test left behind.
  - **A tautology was hiding a mock that never matched the parser.** `use-chat.test.tsx` asserted `messages.length >= 0` twice — true of every array — under the comments "picks up welcome messages" and "should have loaded messages from snapshot". Replacing both with real assertions turned them **red**: the `*/agents/:conversationId` handler returned an invented `conversationSteps: [{ output, actions, quickReplies }]` and no `conversationOutputs` at all, a shape `snapshotToMessages` cannot read (it wants keyed `conversationStep` entries plus a positionally-matching `conversationOutputs`, which is what the backend and the conversation-detail mock both use). **Every conversation load against that handler produced zero messages.** Handler corrected to the real contract; both tests now assert the actual welcome text and the actual snapshot input.
  - **The agents sort test compared only the first and last row**, and its descending half used `localeCompare(...) >= 0`, which also passes when the two are equal — i.e. when the sort did nothing. It now compares the whole rendered column against a sorted copy, and the reversed pass against `[...ascending].reverse()`.
  - **Two lint rules so none of this comes back** (`eslint.config.js`): `no-restricted-syntax` bans `toBeGreaterThanOrEqual(0)` / `toBeGreaterThan(-1)` in any test or spec, and bans `test.skip()` anywhere under `e2e/`. Both verified to fire by feeding them deliberate violations, then removing the probes. **`no-restricted-syntax` is replaced, not merged, across config objects** — the first attempt put the two rule sets in separate blocks and `e2e/*.spec.ts` matched both, so the later block silently won and the tautology rule never reached `rtl.spec.ts`. The selectors are now shared and repeated into both blocks.
  - **One documented exception.** `rtl.spec.ts:175` asserts `hidden.x >= 0` on a viewport coordinate — the bug it pins (`left: -9999px`) made that value large and negative, so it is the whole assertion rather than a tautology. Disabled on the line with the reason, which is the friction the rule is meant to create.
  - **Still open from the review** (steps 2–7): the browser MSW worker still runs `onUnhandledRequest: "bypass"` and `waitForApp()` still swallows its own timeout; nothing validates the 186 MSW handlers against the backend's OpenAPI document; the `integration` tier still does not run on PRs; 22 of 53 routes have no E2E coverage; Workforce bulk delete is still unconfirmed and untested.
- **The whole app shell scrolled, because a 1px checkbox was positioned against the document** (branch: `fix/shell-never-scrolls`): on the parser editor the sidebar, top bar and page slid off the top of the window together, leaving a band of empty background below — the one thing `AppLayout` is built never to do. **5481 passing (353 files)**, +1 (`parser-editor.test.tsx` 40 → 41), plus a new `e2e/layout-scroll.spec.ts` (5 Playwright tests).
  - **The cause is a containing block, not a missing `overflow-hidden`.** `AppLayout` was already `h-screen overflow-hidden` with `<main>` as the only scroller. But `overflow` clips a descendant only when the clipping box sits between that descendant and its **containing block**; an `absolute` element with no positioned ancestor resolves against the initial containing block instead, so every scroll container above it is bypassed. Tailwind's `sr-only` is `position: absolute`, and the parser editor hides each checkbox's real `<input>` behind it inside a label that was not `relative`. Measured in the browser on `/manage/resources/parser/:id`: sixteen such inputs, the lowest at document y=1341 in a 720px viewport, `documentElement.scrollHeight` 1341 vs `clientHeight` 720 — and `input.focus()` (tab into the last checkbox) made the browser scroll the document to reveal it, dragging the shell. After the fix the same page measures 1341 → 720, and focusing the last checkbox scrolls `<main>` by 676 with `window.scrollY` still 0.
  - **Fixed at both levels, because they fix different things.** `relative` on the parser editor's checkbox label puts the input back inside `<main>`'s scroll space, so focus scrolls the page as it should. `relative` on the shell roots — `AppLayout`, all three `WorkforceLayout` viewports, and `AgentStudioPage` (same fixed-height-shell contract, and its inner `overflow-hidden` row was equally bypassable) — makes the guarantee structural: nothing rendered inside a shell can grow the document again, whoever writes it.
  - **The regression test was rewritten after it passed against the bug.** The first version asserted on `<main>`'s scroll position over the mock fixtures and went green with the fix reverted — `par1` renders an empty editor against a live backend, so there was nothing to overflow. The guard now mounts the offending shape itself inside `<main>` (a 3000px spacer, then an `sr-only` input, so the input's *static* position is genuinely below the fold), asserts the document neither grows nor scrolls on focus, and carries two non-vacuity checks (`sr-only` is still `absolute`; the probe really does overflow `<main>`). Verified to fail on the unfixed shell and pass on the fixed one. Four route smoke tests sit beside it, and a unit test pins the label's `relative` class, which jsdom cannot compute from Tailwind.
  - **No i18n or design-sync debt**: no user-visible strings, and the four touched files are either excluded from the design system on purpose (`AppLayout`) or not part of it.
- **The approvals diff compared a formatted document against an unformatted one** (branch: `fix/approvals-config-diff`): a gated whole-document `PUT` rendered every line of the stored config as deleted and the entire proposed body as one added line — on the one screen whose job is to show what actually changes. The same root cause was silently disarming the capability-grant warnings beside it. **5480 passing (353 files)**, +48 (new `redacted-json.test.ts` 30, `resource-diff-viewer.test.tsx` 8 → 19, `escalation-flags.test.ts` 54 → 59, `request-preview.test.tsx` 26 → 28).
  - **The cause is a silent parse failure, not a missing format step.** `ResourceDiffViewer` already parsed, deep key-sorted and re-printed both sides — but its `catch` returned the raw string, so a side that does not parse degrades to a single compact line while the other stays pretty-printed. jsdiff then has no line in common to align and reports a full rewrite. Nothing in the UI said which of the two things had happened, so "this write replaces the whole document" was the only available reading.
  - **The cause, confirmed against the backend source, is EDDI's own redaction.** `SecretRedactionFilter`'s generic rule (`../EDDI/src/main/java/ai/labs/eddi/secrets/sanitize/SecretRedactionFilter.java`) matches `<name>":"<value>` — the key's closing quote, the colon and the value's opening quote **included** — and writes `<name>=<REDACTED>` over the lot, so `"apiKey":"sk-…"` comes back as `"apiKey=<REDACTED>"`: a bare string where a key/value pair was, which no longer parses. A numeric value loses its closing quote as well (`"token":12345678,` → `"token=<REDACTED>,`). The `sk-…` and `Bearer …` rules replace only the value and leave the body valid; this is the one rule that does not. Verified by running the filter's four rules over JSON bodies in Node — the first draft of this fix repaired a shape I had guessed (`"apiKey":<REDACTED>"`) that the filter never produces. `normalizeJson` now re-quotes the real shape (`REDACTED_FIELD`: key position only, so a value that legitimately reads `apiKey=<REDACTED>` is left alone; the key must end in one of the filter's names; an optional tail swallows what a secret with a space leaves behind) **only after a straight parse has already failed**, and if the repair does not yield valid JSON the raw text is used exactly as before. Eight real shapes pinned, plus the two things it must not touch.
  - **When a side still will not parse, the viewer says so.** An amber caveat above the diff ("Not valid JSON — compared as plain text") — a formatting-only difference reading as a rewrite is a conclusion an approver must not reach by accident, and the fix above cannot be assumed to cover every cause.
  - **Unchanged lines fold away.** Three lines of context either side, git-style; a run of four or more beyond that collapses into one clickable "… N unchanged lines" row. Rendering all 400 lines of an agent config back put the reader right where they started — finding the edit by eye. Every fold expands; nothing is hidden that cannot be got back.
  - **Lines wrap instead of hiding behind a horizontal scrollbar.** Each row is now `flex` with a fixed marker gutter and a `whitespace-pre-wrap wrap-anywhere` body: indentation a plain `div` would collapse survives, and an over-long line (a `systemMessage`, or a raw-text fallback) wraps in place rather than pushing every other line off the right edge.
  - **Shared by three callers** — the operator approval preview, the agent import preview and the sync page — so the import and sync diffs get the same normalisation, folding and wrapping. Not a design-synced component (it lives in `components/agents/`, not `ui`/`shared`/`layout`), so no `.design-sync` entry is owed.
  - **i18n**: 4 keys × 11 locales (`importDialog.diffHiddenLines`, `importDialog.diffRawComparison`, `operator.approval.diffStored`, `operator.approval.diffProposed`), translated rather than left as inline fallbacks. The fold count interpolates as `{{lines}}`, not `{{count}}` — `count` would send i18next into plural lookup for a number that is never below four.
  - **Second review pass, three more findings, all fixed.** (1) Expanded folds are keyed positionally, and the set survived a content change — the sync page re-previews into the same viewer instance, so a fold opened in the old diff pre-opened whatever was gap 0 in the new one; the set is now remembered against the diff it belongs to and discarded with it. (2) The header read `Target → Source` on an approval card — vocabulary from the import dialog that means nothing to an approver — and said nothing about which colour is which side. It is now a legend with colour swatches, and the operator preview passes `Stored v{{n}}` → `Proposed`. (3) The legend's first draft used literal `−`/`+` characters, which collided with every test that finds a change row by `getByText("+")`; swatches instead. **Layout verified with the compiled CSS in the browser pane** (the pane could not composite a screenshot this session, so measured via `getComputedStyle`/`getBoundingClientRect` on a throwaway static page under `public/`, removed afterwards): a 720px box shows `scrollWidth` 708 — no horizontal overflow — the 330-character `systemMessage` line wraps to four lines in place, `white-space: pre-wrap` + `overflow-wrap: anywhere` resolve, and the red/emerald/amber tokens flip in `.dark`.
  - **Third review pass — the repair belongs in `lib/`, because a second reader needed it.** `detectEscalationFlags` runs every capability-grant check behind a `JSON.parse`, and its own doc comment justifies the silence on a parse failure ("a non-JSON body is ordinary"). But the filter mangles *every* body carrying a credential-named field with an 8+ character value — including one it has already redacted, because the generic rule runs last and re-matches `apiKey":"sk-ant-<REDACTED>`. So a request that embedded a credential **and** granted `dynamicAgents.allowCreation` warned about the credential alone, and "no second warning" reads as "no capability grant" — the exact false negative that file exists to prevent. The repair moved to `src/lib/redacted-json.ts` (`parseRedactedJson`), used by both readers so they cannot drift, mirroring why `RequestRedactor` holds the backend's three redaction sites in one class. Four regression cases pin the checks now firing behind a mangled field, and one pins that a genuine form post still yields the credential flag alone.
  - **…and four smaller ones.** (1) The caveat said "One side isn't valid JSON" — both can be, and claiming the other parsed is a statement the component cannot make; reworded. (2) The fold button said "click to show" on a control that is keyboard-reachable; now "Show N unchanged lines", with the `…` glyph `aria-hidden`. (3) `→` in the legend is direction, not decoration, and bidi does not mirror U+2192 — it now carries `rtl:-scale-x-100`, so Arabic does not get an arrow pointing away from the side it names. (4) Gap ids are handed out to every dropped run, folded or not, so a short unfolded run appearing above a long one cannot renumber the fold out from under a click; `collapseUnchanged` also lost a non-null assertion in the process. **Re-measured in all three modes** with the compiled CSS: LTR, Arabic RTL (marker gutter on the start edge at x=717 of a 720px box, arrow `scale: -1 1`) and dark — `scrollWidth === clientWidth` in each, no horizontal overflow, and the 330-character `systemMessage` line wrapping to four lines in place.
  - **PR review round (CodeRabbit), two findings.** *Add `data-testid` to the diff rows* — accepted, and it made the tests better rather than merely compliant: rows carry `data-testid="diff-line"` plus `data-diff-kind`, and both suites now assert through a `rowTexts(kind)` helper, so "the threshold change is an ADDITION" is what a test says instead of "this text is somewhere on screen" — the exact distinction between a working diff and a whole-document rewrite, and the one thing a colour class cannot be queried for. *Replace the native `<button>` with the `Button` primitive* — **not taken**, with the reasoning left on the thread. The fold row is a flush full-width row, and `Button`'s cva base (`rounded-lg justify-center h-8 px-3 whitespace-nowrap`) would all have to be overridden at the call site, which is the restyling CLAUDE.md forbids in the same breath; the alternative — a single-use `row` variant in an app-wide, design-synced primitive whose `dtsPropsFor` contract enumerates its variants — is worse for the design system than a native element. The codebase agrees: 630 native `<button>`s across 139 files (vs 66 files importing the primitive), including the "Show the full proposed document" toggle 40 lines away in `request-preview.tsx` and every clickable card in `agent-card.tsx`. What the finding was **right** about is that the row was missing what the primitive would have given it, so it now carries the app's focus treatment — `focus-visible:ring-2 ring-ring`, `ring-inset` so the ring stays inside the diff box — pinned by a test that tabs to it.
  - **PR review round (Copilot), one finding — real, and one I had seen and let slide.** The filter's value class excludes `,`, whitespace, `;`, `{`, `}` and `]`, so a secret containing one is cut at it and the tail is left inside the string: `"password":"abcdefgh,rest"` → `"password=<REDACTED>,rest"`. The repair stopped at that first comma, left `rest"` behind, and the retry failed — so `detectEscalationFlags` again returned `inlineCredential` alone and hid the `allowCreation` grant beside it. My own shape simulation had shown this case failing and I moved past it. The field's extent is its closing quote, not the first delimiter, so the repair now decides **per field** between dropping the debris up to that quote (a string-valued field) and keeping it (a non-string field, whose missing closing quote means the next quote belongs to the FOLLOWING key) — the two are not distinguishable locally, a body can hold one of each, and no uniform reading repairs a mix. Uniform all-drop and all-keep are tried first, then the per-field combinations up to a 10-field cap; `JSON.parse` arbitrates every candidate, so a wrong guess is discarded rather than shown. The greedy scan also had to stop consuming the remnant during matching, because a remnant ending at the next field's opening quote hid that field from the scan entirely. Seven debris shapes, the reported case and a three-field mix are pinned.
  - **Not changed**: the redacted-credential caveat beside the operator diff stays. Masking the stored side to match would remove the noise and also hide a real credential change, which is the wrong trade on an approval surface.
- **Link a dictionary by name, and stop the approval table from running off the screen** (branch: `feat/dictionary-picker-and-layout-fixes`): three reported UI defects, two of them layout, one of them a screen that was never really implemented. **5432 passing (352 files)**, +5 net in `parser-editor.test.tsx` (35 → 40: pick from the list, already-added is refused, manual URI still works, create-and-link, back out of the manual field, Escape closes, name resolution, a non-dictionary URI renders raw with no link — replacing the two that drove the old URI box) and a new `dictionary-uri.test.ts` (9).
  - **Adding a regular dictionary asked the operator to type an id nothing shows them.** The field wanted `eddi://ai.labs.dictionary/dictionarystore/dictionaries/<id>?version=<v>` — an id that appears in no list, no picker and no detail page, so the only way to fill it was to open the Dictionaries list in another tab and copy one out. A typo saved cleanly and resolved no dictionary at runtime. Replaced with `DictionaryPickerDialog`: search the store, pick by name, or **create one on the spot** (name + description → `POST` → linked immediately, the same create-or-pick shape as `AddExtensionDialog`'s config step). The URI field survives as "Link by URI instead" — pinning a version, or pointing at a resource the listing cannot reach, is still legitimate — and it is now the third choice rather than the only one.
  - **Rows say which dictionary they link, read one descriptor at a time.** Every row used to read "Regular Dictionary" over a raw id; it now shows the descriptor's **name** over `id · v{n}`, with an external-link button to the dictionary's own page (a new tab, not a route change: this editor also renders inside the workflow dialog, where navigating away discards unsaved edits). The name comes from `GET /descriptorstore/descriptors/{id}?version={v}` (new `getDescriptor`), **not** from the store listing the picker browses — verified against a live backend where `/dictionarystore/dictionaries/descriptors` returns `[]` for dictionaries whose individual descriptors resolve perfectly well, which would have rendered every row as a raw `6a84cdfb…`. `useResourceDescriptors` gained an `enabled` flag so the picker, which stays mounted, lists nothing until it is opened.
  - **The approvals queue pushed its own columns off-screen.** Expanding a TOOL_CALL pause put `ApprovalBanner` in a `colSpan={6}` cell, and an auto-layout table sizes columns from their content's min-content width: the redacted-arguments `<pre>` is one very long line, so the table grew to **9010px** inside a 966px card and every column after ID was clipped away. The `<pre>`'s own `max-w-full` cannot help — nothing inside the cell has a definite width to resolve against. The expanded cell's content now sits in a `w-0 min-w-full` wrapper, which contributes nothing to the column calculation and then fills the width the *other* rows sized; measured 9010px → 964px, inner panels still scrolling within themselves.
  - **…and on a phone the decision buttons were unreachable.** Same screen, separate defect: the card was `overflow-hidden`, so at 375px the 743px table was simply cut off with Review/Approve/Reject sitting ~350px past the card edge and no way to scroll to them. Now `overflow-x-auto` — the table scrolls inside its card, the page itself still does not.
  - **The operator composer looked pre-filled before anyone typed.** In the top-bar drawer the textarea is ~210px wide, where "Ask about agents, conversations, deployments, logs…" wraps to three lines, overflows the 40px box and shows a scrollbar on an **empty** input. `placeholder:truncate` keeps the resting state honestly single-line (`scrollHeight` 80 → 40); the full text stays in `aria-label`, the wide operator page still renders it whole, and typing multiple lines still grows the box to 120px and scrolls as before.
  - **Verified in the running app against a real backend**, not only in jsdom: created a dictionary through the picker (toast, dialog closes, row appears with its name and a working link), confirmed the created document (`{"words":[],"regExs":[],"phrases":[]}`) and its descriptor, then deleted both verification dictionaries so the instance is as it was. Approvals measured expanded at 1280px and at 375px; the picker's list, empty, create and manual states all exercised.
  - **New i18n**: 16 keys × 11 locales (`dictionaryPicker.*` plus `parserEditor.regularDictsHint` / `openDict`), translated rather than left as inline fallbacks. `parserEditor.regularDictLabel` deleted from all eleven — rows carry the dictionary's own name now, and the i18n gate does not check for orphans in `en.json`, so nothing else would have caught it.
  - **PR review round (CodeRabbit + Copilot), six findings, all real, all fixed.**
    - **`parseDictionaryUri` took the last path segment of anything** (both reviewers, independently). `new URL` resolves free text and every other store's URI just as happily, so a ruleset URI pasted into the manual field became a dictionary "id" — which the row then turned into a `/manage/resources/dictionary/beh1` link to nothing, a descriptor request that cannot resolve, and a duplicate-detection key matching the wrong row. Only a `<store>/<plural>/<id>` tail is an id now; anything else renders as the raw URI with no link, which is what the helper's own doc comment always claimed. Nine cases pinned in `dictionary-uri.test.ts`, including the collection URI ending in `/dictionaries/` that Copilot named. Re-verified the create flow against the live backend afterwards, since the `Location` header goes through the same parser.
    - **A failed rename left an orphan dictionary** (Copilot). `useCreateResource` awaits the descriptor PATCH *inside* the mutation, so a PATCH failure rejects a call whose POST already succeeded: the dictionary existed, unnamed and unlinked, and the obvious retry made a second one. The dialog now POSTs bare and renames in its own try/catch — a rename that fails downgrades the toast to a warning and still links what was created. A local `creating` flag covers both calls, because `mutation.isPending` re-enabled Create the moment the POST resolved.
    - **The id/version lived only in a `title`** (Copilot). It is the only thing telling two same-named dictionaries apart, and a tooltip says that to a mouse and to nobody else — now a rendered line in the row.
    - **`aria-label` on the operator composer contradicted its own placeholder** (CodeRabbit). Paused and read-only states set state-specific placeholder text while the label stayed the generic "Ask about agents…" — so the one state that explains *why* the composer is closed reached sighted users only. Both now derive from one `composerPlaceholder`.
    - **A test named for something it did not check** (CodeRabbit). "closes the picker without adding anything" clicked the manual field's Cancel, which backs out to the list — the dialog stays open. Renamed to what it verifies, plus a real Escape-closes-the-dialog case.
    - **Nitpicks**: `getDescriptor` now encodes the id segment and builds its query with `URLSearchParams` (the id can come from a hand-typed URI); Thai used a second word for "parser" in the new strings where `parserEditor.title` had already settled on `ตัววิเคราะห์`.
  - **Responsive pass over the touched surfaces afterwards found one more, in a shared primitive.** `AccessibleDialog` centred its box with no height cap, so any dialog taller than the window was clipped at **both** ends — the picker with a full list at 900×480 lost its title and close button off-screen with nothing to scroll. It is now a flex column capped at `calc(100dvh-2rem)` with a fixed header and a scrolling body, which fixes the same latent bug for all seven of its callers; the picker's list also caps at `min(18rem,40dvh)` so it shrinks before that safety net is needed. Checked at 375×812, 380×360, 900×480, 768 and 1280, in Arabic RTL (search icon on the start edge, `padding-inline-start` from `ps-9`, nothing escaping the dialog) — no horizontal page overflow anywhere.
- **Workforce wizard: say up front what `agents/setup` will refuse, and read its answer when it does** (branch: `claude/workforce-template-agent-prompts-bd9cf9`): creating a Workforce failed with `[object Object]` when an advisor had no system prompt; the only readable copy of the reason was the server log. **5409 passing (350 files)**, +33 new (19 wizard, 3 ApiClient/chat, 11 i18n guard).
  - **Three defects stacked into one unreadable failure.** (1) `POST /administration/agents/setup` validates in `AgentSetupService.setupAgent`, in order: blank name, blank `systemPrompt`, then — for every provider that is not local (`isLocalLlmProvider`: ollama, jlama, bedrock, oracle-genai) — a missing API key; a blank provider resolves to `DEFAULT_PROVIDER` = anthropic, so "no provider, no key" is the *common* failure, not the safe default. `RestAgentSetup` answers `400 {"error": …}` and logs the same sentence. (2) That text reached `ApiError.message` fine (`extractErrorMessage` reads the `error` key), and the wizard then dropped it: `err instanceof Error ? err.message : String(err)`, against a **plain-object** `ApiError` — `String({…})` is `[object Object]`. (3) "Try Again" remembered which *rows* had finished but not the ids they were given, so a retry skipped the setup call and then built the group with `agentId: ""` for them — a broken group, not a resumed one.
  - **The durable fix for (2) is in the client, not the call site.** `ApiClient` now throws `ApiClientError extends Error implements ApiError` (`api-client.ts`; `chat.ts` was the only other place hand-rolling the literal). `isApiError` is a shape check and `getErrorMessage` unchanged, so nothing reading `status`/`url` moves — but the `instanceof Error` unwrap that `group-wizard.tsx` (4 sites), `secrets.tsx`, `variables.tsx`, `secret-key-picker.tsx` and `agent-config-sections.tsx` all use now reads the backend sentence instead of `[object Object]`. Full suite green with the change; `api-client.test.tsx` pins the instance-of and both unwrap shapes.
  - **The team step validates what the server will.** `memberIssue(member, defaults)` in `wizard/member-validation.ts` (own module so `team-builder.tsx` keeps exporting components only) returns the first of: no name; `existing` with no agent picked; `new` with no system prompt; `new` whose effective provider needs a key and has none. **Next stays clickable** — a disabled button said *that* something was missing but never *what*, and the field it was waiting on sat inside a collapsed card. A failed click reveals the messages, holds any card with a flagged field open (derived, so the field is in the DOM the same render — plus a latch, because without one the first keystroke into a flagged prompt resolved the flag and collapsed the card under the cursor), marks it `aria-invalid`/`aria-describedby`, and **moves focus to the first flagged control** (ref-scoped `querySelector`, the same pattern as `agent-card`/`operator-drawer`). Validation is live: filling the prompt surfaces the key requirement without another click. Back off the step clears the flags so a freshly picked template does not arrive pre-flagged.
  - **A shared "LLM for new advisors" block** (provider / model / key, defaulting to anthropic + its default model, hidden when nothing will be created) — without it a five-role template meant setting provider and key five times over. Per-advisor fields become overrides whose controls say what they inherit ("Workforce default (Anthropic)"). When advisors are waiting on the shared key, the *shared block* is flagged too, so focus lands on the one field that fixes all of them. Local providers hide the key field, matching the backend.
  - **Templates seed a starter system prompt per role** (`starterPrompt(name, role, t)`, i18n'd like the role names it is built from, in four shapes so a blank name or role never leaves "You are , the…"); a custom advisor gets an "Insert a starter prompt" button while its prompt is empty. The review step now shows, per new advisor, the effective provider · model and a two-line prompt preview — the last place to notice a wrong model or a template prompt that was never edited. Advisors an earlier attempt already provisioned show as "Created".
  - **Retry actually resumes.** Each minted `agentId` is written back into `members`; the loop skips anything that already has one (existing *or* previously created); Back from a failed review drops the stale progress list so the summary shows again. On the team step such an advisor is badged "Created" and its card shows a note in place of the prompt/LLM fields — an edit there would silently not apply, so it is not offered. Mutation-checked: without the write-back the retry test creates Ana twice and the group carries `""` for her.
  - **Verified in the running app** (MSW mock mode): template → Next lands focus on the shared key input with six alerts (one shared, five per-advisor); key → review lists provider · model and prompt per advisor → Create navigates to the new board. RTL at 375px in dark mode: no horizontal overflow, focus on the first flagged textarea, starter buttons present.
  - **Tests**: 14 in `workforce-wizard-validation.test.tsx` (missing prompt named; card held open + focus + collapse disabled; live move to the key requirement, shared block flagged and focused; shared key satisfies all; local provider needs no key; held-open card stays open once its flag clears; board name flagged + focused; flags do not survive Back + new template; template seeds five prompts; starter button fills from name/role; blank name → role-only starter; 400 renders the backend text; Try Again reuses created advisors and the group carries real ids; Back after failure shows "Created" + note on the team step and the summary again). i18n: 18 keys × 11 locales, translated rather than left as inline fallbacks (`promptRequired`, `nameRequired`, `agentRequired`, `apiKeyRequired`, `boardNameRequired`, `personalityHint`, `useDefault`, `useDefaultKey`, `useStarterPrompt`, `starterPrompt`, `starterPromptNoRole`, `starterPromptRoleOnly`, `starterPromptBare`, `llmDefaults`, `llmDefaultsDesc`, `llmDefaultsKeyRequired`, `alreadyCreated`, `alreadyCreatedNote`).
  - **PR review round (CodeRabbit + Copilot), six findings, all real, all fixed.** Two were bugs this PR introduced, two were holes it left open, two predate it on lines it touched.
    - **`agentId` carried two meanings, and the mode toggle did not clear it** (both reviewers, independently). Picking an existing agent and then switching the card back to "Create new advisor" left the id behind: `memberIssue` returned null at its `if (member.agentId)` branch, so the advisor never owed a prompt or a key, the loop skipped setup, and the group was assembled with the *picked* agent — badged "Created", with no error. Fixed by splitting the field rather than clearing on transition: `agentId` is only ever the user's pick, `createdAgentId` only ever what a failed attempt minted. Clearing on toggle would have fixed this direction and broken the other (toggle away and back after a partial failure → duplicate agent). Mutation-checked by restoring *both* the merged read and the merged badge.
    - **The shared key was sent to providers that take none.** `effectiveLlm` inherited `apiKey` regardless of provider, so entering the team's Anthropic key and then switching to Ollama hid the field but still put the secret in the setup request — where the backend vaults it and writes a reference into an LLM config with no use for it. `effectiveLlm` now drops the key when `providerNeedsKey` is false. Verified against real requests in the browser, not only in jsdom: five setup calls, `apiKey` absent on every one, the secret never leaving the page.
    - **`gemini-vertex` — the deliberate exception was the wrong call.** The wizard's whole job here is to mirror what the server will refuse, and it was reading `LLM_PROVIDERS[].needsKey`, a *UI* flag that decides whether to render a key field. It marks `gemini-vertex` keyless; `isLocalLlmProvider` does not, so that one provider sailed through into the exact late failure this screen exists to prevent. `providerNeedsKey` now mirrors the backend's allow-list (`BACKEND_KEYLESS_PROVIDERS`) instead. The `needsKey` flag keeps its own job elsewhere.
    - **`chat.ts` still threw away backend messages.** Its two raw-fetch paths (text/plain body, SSE stream) bypass `ApiClient` and reported only `response.statusText`, so `400 {"error":"…"}` arrived as "Bad Request" — the same defect this PR set out to fix, one file over. `apiErrorFromResponse(response, fallback?, url?)` is now exported from `api-client.ts` and used by `ApiClient.request` and both chat paths, so there is one place that knows how to turn a failed `Response` into an error. Streaming keeps "Streaming failed: …" as the fallback only.
    - **`SecretKeyPicker` swallowed the label.** It renders its own input, so the wizard's `<label htmlFor="apikey-…">` resolved to nothing: the required marker and the error were announced to nobody, on the very field the wizard had just moved focus to. It now accepts and forwards `id` / `aria-invalid` / `aria-describedby` (to the input in the direct-entry state, to the chip as a `role="group"` in the vault-reference state). It is a design-synced component, so `.design-sync/config.json`'s hand-written `dtsPropsFor` entry was updated with it (CLAUDE.md). **`AgentPicker` had the identical defect two lines away** — same shape, its own input, the same orphaned `htmlFor` on "Select agent *" — and neither reviewer flagged it; fixing only the reported instance would have left its twin, so it got the same three props and the same test.
    - **Spanish called the Workforce a "plantilla" — which is also the wizard's Template step.** Step 1 read "Plantilla" while step 2 asked for the "Nombre de la plantilla", and my "Predeterminado de la plantilla" read as a *template* default. The rest of the namespace (`Workforce.board.*`, `Workforce.settings.*`) already said "grupo de trabajo", so the nine outliers were realigned to it rather than to a third new term. Spanish was the only locale with the collision; a new `i18n-quality` case now asserts across all eleven that the Workforce strings do not contain `stepTemplate`.
- **Claude Code UI docs + design-sync app chrome** (branch: `docs/ui-handoff-and-design-sync`): Docs, design-sync build config, and the UI-layer fixes the docs work surfaced (double padding, missing error branches, a dead token). **5048 passing (335 files)** with `main` merged in.
  - **Four docs.** `CLAUDE.md` (loaded every session) defers to `AGENTS.md` and routes to three on-demand skills: `.claude/skills/eddi-ui` (component inventory, variant props, token table), `eddi-screens` (shell contract, page skeleton, state ordering, `ConfigEditorLayout`, wizards), `eddi-data` (routing, Query hooks, `ApiClient`, i18n, MSW, gates).
  - **Doc corrections found by verifying against the code**, not by trusting the handoff: the handoff counted `view-mode.ts` (a localStorage helper, not a component) toward `shared/`, inflating the `ui`+`shared` surface by one — it was 24, not 25, at the time (it is 25 now that `main` has added `UpdateCheckCard`, which is a real component); the page skeleton documented `channels.tsx`, which is the outlier — the real convention is `space-y-6` (29 of 40 pages, vs 2) with a `text-3xl` heading and an `h-8 w-8` icon, so the reference is now `agents.tsx`; and `eddi-data` recommended `tsc --noEmit`, the exact no-op `AGENTS.md` §2 warns about (pre-commit runs `npm run typecheck`).
  - **Design-sync surface 21 → 29**: adds `Sidebar`, `TopBar`, `PlatformStatus`, `PageLoader`, `MockDataBanner` (the chrome) plus `DropdownMenu`, `ModeSwitcher`, `RefetchErrorNotice` — all three had always been inside the declared `ui/`+`shared/` surface but were missing from `ds-entry.tsx` until this PR, which adds them. `AppLayout` and `ConfigEditorLayout` stay excluded — both pull Monaco into the bundle.
  - **The sidebar-token bug is real, but the patch notes had the numbers wrong.** They claimed the scoped build ships *zero* `--color-sidebar*` in `:root` and that `@source .../layout` restores *five*. Measured across full CLI builds: nothing scanned → 0 of 5; `ui`+`shared` (pre-patch) → **2 of 5**; `ui`+`shared`+`layout` → **5 of 5** (4 of 5 before the avatar fix below). Tailwind v4 tree-shakes `@theme` tokens, so a token ships only if a scanned file uses a utility reading it. The two that survived pre-patch were held up by `shared/mode-switcher.tsx` alone; `border-sidebar-border` and `fill-sidebar-accent` live in `layout/sidebar.tsx`, which is what the line restores. It also emits the layout utilities themselves (~8.7 KB; 45.4 → 54.3 KB) without which a synced `Sidebar`/`TopBar` renders unstyled.
  - **Double-padding fixed**: the 8 pages that added their own `p-6` under `AppLayout`'s already-padded `<main>` (`channels`, `channel-detail`, `coordinator`, `logs`, `orphans`, `schedules`, `secrets`, `variables`) now render at 24px, not 48px. Verified in the running app — `main` 24px, page root 0px. (`logs` was caught last, by PR review — the original sweep's grep pattern missed its `flex h-full flex-col p-6` root.)
  - **Every page that loads data now has an error branch.** The five that did not were each falling through to something misleading: `channels` and `coordinator` showed an empty state ("No channels yet", "the service may still be starting up") on a failed fetch; `group-wizard`'s nested-group picker showed "No groups available", which reads as a fact about the account; `agent-studio` rendered its normal chrome with an empty pipeline, indistinguishable from an agent with no workflow; and `gdpr` showed the **green "Processing Active" badge** when the restriction check failed — reporting an unknown legal-hold state as a known-safe one. That one also now disables the restrict/unrestrict toggle, since both its label and its action derive from the value that failed to load. Shape per surface: `ErrorState` where the container can be replaced, `RefetchErrorNotice` for the inline picker, a neutral "unknown" chip where the value drives a decision. New key `gdpr.statusUnknown` translated across all 11 locales.
  - **A test was pinning one of those bugs.** `coordinator.test.tsx` had `it("shows empty state when coordinator status API fails")` — asserting the exact wrong behaviour. Replaced with the corrected expectation rather than deleted, plus negative assertions so the fall-through cannot come back.
  - **`--color-sidebar-accent-foreground` is no longer dead.** The sidebar avatar paired `bg-sidebar-accent` with `text-sidebar`; it now uses the token's actual partner (identical in light `#ffffff`, one shade off in dark `#0c0a09` vs `#09090b`, so no visible change). That single call site takes the design-system bundle from 4 of 5 sidebar tokens to **5 of 5** — and is the only thing keeping it there.
  - **`.design-sync/` is now type-checked.** New `tsconfig.design-sync.json`, referenced from `tsconfig.json`, maps `eddi-manager` → `ds-entry.tsx` and pulls in `src/vite-env.d.ts`. Proven to work by temporarily passing `collapsed="yes"` to the `Sidebar` preview: `tsc -b` failed with TS2322, where previously a wrong prop survived until someone ran a sync.
  - **`channels` persists its view mode** via `getStoredViewMode`/`setStoredViewMode` — it was the only one of the 6 `ViewToggle` pages that reset on every visit.
  - **Re-sync completed (2026-08-11)**: all 29 components are uploaded to the Claude Design project ("Design System", org default) and verified end-to-end — a test generation built the full Agents screen from the bundle (Sidebar + TopBar shell, gold-token Buttons/Badges/Cards, EmptyState/ErrorState). Three defects fixed on the way: every component had shipped an **empty API contract** (`[key: string]: unknown` — the converter's `.d.ts` extractor finds nothing in an app repo; `cfg.dtsPropsFor` now hand-carries all 29, so **adding a component now requires a `dtsPropsFor` entry too**); `Sidebar` threw `__APP_VERSION__ is not defined` in the bundle (Vite `define`, shimmed in `ds-entry.tsx`); and the expanded sidebar's wordmark was a broken image (`/logo_eddi.png` moved from `public/` to an imported `src/assets/` asset, inlined by both builds).
- **EDDI Update Check** (branch: `fix/update-check-screen-and-csp`): Opt-in "is a newer EDDI released?" screen at `/manage/updates`, plus the banner it can raise.
  - **It never worked outside `npm run dev`, and no test could have caught it.** EDDI serves the Manager under `connect-src 'self'` (`application.properties`, the `csp-default` filter), so the browser refused the GitHub request in every real deployment — the card reported "Could not reach api.github.com. Check your network or any outbound proxy," pointing at everything except the header that had to change. Invisible to the suite by construction: MSW intercepts at the fetch layer, where CSP does not exist, and the dev server sends no CSP at all. **Two fixes.** (1) EDDI's `csp-default` now allows `https://api.github.com` in `connect-src` — the request is read-only, unauthenticated, `no-referrer`, one public endpoint, and the check stays opt-in. *(That edit is in the EDDI repo, not this one.)* (2) A CSP-blocked fetch rejects with the same `TypeError` as a dead network, so the Manager now listens for `securitypolicyviolation` on the blocked origin and reports `blocked-by-csp` — naming the directive to change instead of blaming a proxy that was never in the path. Both verified in a real browser by serving `dist` behind EDDI's exact header: strict → the CSP message, `+ https://api.github.com` → a live check returning 6.2.0 with notes.
  - **`font-src 'self'` was blocking six fonts, for the same reason.** Vite inlines assets under 4 KB as `data:` URIs and six Noto subsets landed under it, so those faces were refused in production and fell back to a system font while the other 467 loaded. `build.assetsInlineLimit` in `vite.config.ts` now refuses to inline `.woff2` at any size; the build emits 473 font files and zero `data:font` URIs, and the CSP console errors are gone.
  - **PR review round (Copilot + CodeRabbit), and one test that could not fail.** Copilot found nothing here and two things on the EDDI side (an `InfrastructureIT` that asserted only `script-src`, and a missing changelog entry — AGENTS.md §8). CodeRabbit found five on this PR, all real. The load-bearing one: `watchForCspBlock` matched the blocked origin by *prefix*, so a violation against `https://api.github.com.example` — an unrelated host — would have reported a genuine GitHub outage as `blocked-by-csp`, sending the operator to edit a header that was never the problem. Exact origin or `origin + "/"` now, the two shapes browsers actually report; the same class of bug it caught in the EDDI assertion. Next: the banner's route guard compared `pathname` verbatim, so `/manage/updates/` — served from the same route — showed the banner on the page it exists to stay off. **And the test for that guard could not fail**: it asserted the banner's absence while the check was still pending, when the banner is absent anyway, so it passed with the rule deleted. It now renders the card alongside and waits for the release to be named first. Mutation-checked three ways: guard neutralised → both route cases red; normalisation dropped → only the trailing-slash case; prefix match restored → the lookalike-origin case. Plus two nitpicks: the Spanish message used formal address in an informal namespace (de/fr/pt checked, already consistent), and the page test asserted through roles and localized text where AGENTS.md §247 requires `data-testid`.
  - **Review pass over this branch, against the running build rather than the source.** What it found: the document-title map had no `updates` entry, so the tab and every history entry read `updates — EDDI Manager` in raw lowercase — the exact defect that file's own comment records having fixed for nine other sections (WCAG 2.4.2). The banner was still rendered on the page it links to, repeating the version and pointing "How to update" at the reader's current location; it now returns null there. The German nav label read `Aktualisierungen` while its own page title said `EDDI-Updates` — the only locale of eleven where the menu entry and the heading disagreed. The card's failure copy had grown to a four-deep ternary, now an `ErrorText` switch whose generic message is the `default`, so a reason added later degrades to advice rather than to an icon with no text. And the page ran full-bleed, which on a wide screen strands the button an inch from the versions it belongs to and sets release-note prose at ~1000px a line — capped at `max-w-3xl`. Verified afterwards in a real browser at desktop, at 375px, in dark mode and in Arabic RTL: both the blocked and the working states, no horizontal overflow in any of them.
  - **It is its own screen, not a dashboard section.** It shipped as a card at the bottom of the dashboard, which put a deployment chore — one an operator goes *looking* for, a few times a year — below everything they open the dashboard to see, and made it reachable only by scrolling. Now a page under **Admin** in the sidebar (`UpdatesPage`, which owns the heading; `UpdateCheckCard` is its body and no longer renders a title of its own). The nav entry is the last item of the last section, so the sidebar's version line — the one place in the app that already says which release you are on — links to it as the shortcut, with an `aria-label` that names the destination since the visible text is only a version string. The banner links to the route instead of `/manage#updates`, so the dashboard's hash-scroll effect and its three deep-link tests are gone with it — a route needs no scroll shim.
  - **Both sources shown separately, but only one is fetched.** The card shows three versions — installed, GitHub release, Docker image — each linking to its own source. The Docker image is **derived** from the release version, not looked up.
  - **Why deriving is correct, not a shortcut.** In EDDI's `.github/workflows/ci.yml` the `release` job declares `needs: docker`, so the image is pushed to Docker Hub *before* the GitHub release is created, every time — the release body even embeds the exact `docker pull` command. A published release therefore implies a published image at the release version. Verified against reality as well as against the workflow: of 49 published releases, 48 have a matching registry tag, and the one that does not is `6.0.0-RC1`, a prerelease — which `releases/latest` never returns, being defined as the newest non-prerelease non-draft release.
  - **No third party, and a test that keeps it that way.** An earlier revision read Docker Hub through `img.shields.io`; that was rejected. Worth recording so nobody re-derives it: every first-party Docker endpoint is CORS-blocked from a browser — `hub.docker.com/v2`, `registry.hub.docker.com/v2`, `index.docker.io/v1`, `auth.docker.io` and `registry-1.docker.io` all answer a request carrying `Origin` while sending **no** `Access-Control-Allow-Origin`, and Docker Hub rejects the preflight with 405 (`allow: GET, HEAD`). So anything that appears to make a live Docker check work from the browser is a relay. `api.github.com` is now the only off-origin host the Manager contacts; `update-check-card.test.tsx` asserts exactly that, and MSW's `onUnhandledRequest: "error"` fails the suite if a new host appears. If a live Docker check is ever genuinely needed, the only first-party route is a same-origin proxy on the EDDI backend.
  - **Release notes behind a gift icon.** GitHub's `body` rendered with `react-markdown` + `remark-gfm`, collapsed by default, deliberately **no** `rehypeRaw` — release notes are third-party text, so raw HTML stays escaped (there is a test that pins this).
  - **Off unless asked.** No request leaves the browser until the operator presses *Check now* or ticks the checkbox. Ticking it stores `eddi-auto-update-check` in localStorage and runs exactly one check per browser reload (`staleTime: Infinity` on a QueryClient that lives as long as the page), which is what "check on load" means here.
  - **`updates.ts` is the deliberate exception to the raw-`fetch` rule in AGENTS.md §3.** Every other raw fetch must spread `api.getAuthHeader()`; this one must not — the request goes to github.com, and attaching this deployment's Keycloak token would leak it. Test asserts no `Authorization` header is sent.
  - **The preference is an external store, not `usePersistedBoolean`.** The checkbox (Updates page) and the banner (app layout) are separate subtrees; with per-component state the banner would keep a stale `false` and stay hidden until the next reload despite the check having run.
  - **Comparison never guesses.** Semver-aware, including prerelease precedence, so `6.3.0-SNAPSHOT` sorts below `6.3.0` and above `6.2.0`. An unparseable version (including the `"Unknown"` the version endpoint falls back to) yields `unknown`, never `up-to-date`. A deployment ahead of the latest tag is reported as ahead rather than current.
  - **Update instructions** are EDDI's own (`eddi update`, or the `docker compose pull && up -d` pair from `~/.eddi`), shown only once there is something to update to.
  - `useEddiVersion` extracted so the sidebar footer and this card share one `/openapi` read instead of two.
  - **"Docker Hub" is a literal, not an i18n key.** It is identical in all 11 locales, so a key reads to `i18n-quality`'s debt ratchet as ten untranslated strings. Brand names stay out of the locale files.
  - **Review pass, done against the running app rather than the source.** `vite.config.ts` now honours `PORT` (it hardcoded 3000, so two worktrees could not run dev servers side by side, which is what blocked looking at this at all). What the browser found that jsdom could not:
    - **The banner's "How to update" link worked exactly once.** The effect was keyed on `hash`; a second click leaves the hash at `#updates`, so it never re-ran and the link silently stopped scrolling. Keyed on `location.key` as well — React Router mints a fresh one per navigation, including the `replace` it performs when the target equals the current location. Mutation-checked: the regression test fails with `[hash]` restored.
    - **Real release notes are ~23 KB with GFM tables.** Poured into a nested 320 px scroll box that is dozens of screens of scroll-trapping to say what a link says better, and a wide table stretched the card. Notes are now cut to a ~1500-char preview on a paragraph boundary (backing off past an unclosed code fence), and `[&_table]:block` keeps a wide table scrolling inside its own box — verified by injecting a six-column table into the live prose node.
    - **`<conversationId>`, `<tool>` and `<meta>` appear in the notes as prose placeholders, not markup** — which makes the no-`rehypeRaw` decision load-bearing rather than merely cautious: a raw-HTML pass would swallow them. Pinned by a test.
    - The image-availability rows sat **outside** the `aria-live` region — the most actionable line in the card was the one a screen reader never heard. Moved in, and the region gained `space-y-3`. (Those rows are gone with the second lookup; the `space-y-3` stays, since the region can still hold more than one child.)
    - A missing Docker version rendered a **bare em-dash wearing an external-link icon** — a link to nothing. The version cell is only a link when there is a version to link to.
    - `Published 1/15/2026` glued onto the end of a sentence with no terminal punctuation. Now its own line via `dateStyle: "medium"` — this app ships both month-first and day-first locales, where `3/4/2026` is two different days.
    - **Copy buttons on the commands.** The `docker compose` line is 403 px in a 251 px box on a phone; without copy, following the instructions means horizontally scrolling a code block and retyping it.
    - `aria-controls` pointed at the notes panel's id while the panel was collapsed and the id did not exist.
  - **Checked and found *not* to be ours**: RTL at mobile width scrolls the dashboard horizontally, but `/manage/agents` (which has no update card) reports an identical `scrollWidth`, so it is pre-existing. Worth a separate look.
  - **Tests**: 59 new (28 lib — parse/compare/notes-preview/tag-derivation plus rate-limit vs unreachable vs malformed; 21 card, including the single-host guard; 7 banner; 3 dashboard deep-link). i18n: 27 keys × 11 locales. After the move: the 3 deep-link tests are replaced by 3 on `UpdatesPage`, 1 on the dashboard (it must *not* carry the card) and 1 on the sidebar (the entry links to `/manage/updates`); i18n gains `nav.updates` × 11. After the CSP find: 3 lib tests (CSP-blocked vs unreachable, a violation against another host, and no leak into the next check) and 1 card test for the message; i18n gains `updates.errorBlockedByCsp` × 11.
- **Group Collaboration Wave Parity** (branch: `feat/group-collaboration-wave-parity`): Closes the gap opened by EDDI PR [#626](https://github.com/labsai/EDDI/pull/626) (`refactor/group-service-split`, merged to EDDI `main` 2026-08-07), which shipped Wave 0 (F1–F6) + Wave 1 (I1–I4) + Wave 2 (I5, I7). A full endpoint- and model-level sweep of EDDI `main` against this repo found the drift confined to group collaboration — agents, channels, HITL/tool-approvals (incl. #627 request pinning), quotas, variables, user-memory, secrets and schedules were already aligned. Includes:
  - **Two live bugs.** (1) `ENTRY_TYPE_INFO[entry.type]` was indexed unguarded and `.label` dereferenced, so a transcript containing any of the eleven entry types F4 added threw a TypeError and blanked the view — including `FOLLOW_UP`, which the Manager itself produces via `followupGroupMember`. Replaced by `entryTypeInfo()`, which degrades to a humanized label. (2) `AgentGroupConfiguration.LifecyclePolicy` is the one group enum carrying Jackson's `@JsonValue`, so the backend writes `"keep-deployed"`; the Manager typed it `"KEEP_DEPLOYED"` and fed it straight into a `<select>`, so a saved lifecycle never round-tripped. Normalized in `getGroup` and at every read site.
  - **Config parity**: `recordDissents` (I4), `taskListConfig` (I5), `protocol.maxCostPerDiscussion` + `onCostExceeded` (I1), per-phase `convergence` + `allowAbstention` (I2/I4), `dynamicAgents.maxDelegationDepth`/`delegationTimeoutSeconds`/`allowedDelegationTargets` (I7/F18), and the backend's `MAX_MEMBERS`/`MAX_DISCUSSION_ROUNDS` bounds.
  - **New `GroupPhaseEditor`** for per-phase abstention and convergence, mounted beside the approval editor in the group config panel. Materializes a preset group's phases the same behaviour-preserving way the approval editor does, and carries `requiresApproval` through untouched so the two compose. The two are now mutually exclusive — both write the whole config at one version.
  - **Runtime visibility**: `DecisionRecord` (F3) rendered as a verdict card with winner, tally and minority report (and an unparsed judgment kept verbatim rather than hidden); the F5 cost ledger on the transcript header with a per-member breakdown; convergence progress on the phase header; `TaskItem.createdByAgentId` as filed-by attribution on the task board; transcript signature-envelope fields.
  - **Two save-time rules the backend only logged**: a phase restricted to a moderator the group does not name, and a non-positive cost ceiling (which the backend silently coerces to *unlimited* — the opposite of what it reads as, so the editor refuses it).
  - **SSE**: `convergence_checked`, `convergence_reached`, `decision_reached`. (`token` and `synthesis_complete` are declared in `GroupConversationEventSink` but have no producer, so they are deliberately not modelled.)
  - **Group discussion attachments** — `DiscussRequest.attachments` was supported by the backend and never sent; the input now stages inline base64 files (hidden on a continuation, which the backend rejects). Closes "File upload for group chat" from the wishlist below.
  - **Review-pass finds**: the delegation allow-list re-serialized its parsed list into the field on every keystroke, making a separator impossible to type; an abstention's deliberately-null content rendered as "No response", reporting a deliberate pass as a failed turn; `PHASE_CLASSES` in the analytics phase bar was an exhaustive `Record` over a growing backend enum.
  - **Second review pass** (code + UX, the latter done against the running app rather than the source):
    - **`npx tsc --noEmit` checks nothing in this repo** — `tsconfig.json` is a solution file (`"files": []` plus two references), so it resolves zero inputs and exits 0. The pre-commit hook ran exactly that and had been passing everything; a syntax error committed cleanly and only CI's `tsc -b` caught it. Hook now runs `npm run typecheck`, and AGENTS.md §2 no longer claims the no-op form is a full check.
    - **`lib/utils` had become non-leaf.** `formatUsd` imported `@/i18n/config`, which touches `document.documentElement` at module scope — so every consumer of `cn()` ran the i18n bootstrap. Reads `<html lang>` instead (which `i18n/config` sets on init and on `languageChanged`), so an in-app language switch still works. Verified live: `de` renders `2,50 $`, `ar` renders `‏2.50 US$` under `dir="rtl"`.
    - **Two more transcript renderers.** The Workforce surface renders group transcripts through `board-transcript` and `conversation-viewer`, independently of the Manager's card. Both guarded their `ENTRY_TYPE_INFO` lookup, so they never crashed — they silently rendered *no badge*, which for a DISSENT means an unlabelled contribution indistinguishable from an ordinary opinion. All three now share `entryTypeInfo`. Mutation-checking showed 4 of the 5 new tests here pass against the old code too (the map entries, not the accessor, are what fixes the known types); the test file now says exactly what each case pins.
    - **Stored phases silently override the framework.** `resolvePhases` returns a stored phase list verbatim, and `getStyle`/`getMaxRounds` feed nothing else (verified: three call sites, all preset expansion) — so once phases are materialized, the Collaboration Framework and Max Rounds controls are inert. A user could change the framework, save successfully, and watch the old flow run. The settings page now says so, next to the two controls, and links to the phase editor.
    - **An inert convergence block could not be cleared.** A config authored via the API can enable convergence on a phase that runs once; `disabled={!canConverge}` rendered that checkbox checked *and* frozen. Disabled now only when there is nothing to turn off, with an amber note distinguishing "inert" from "unavailable".
    - **Unenforced backend limits and dead helpers.** `MAX_GROUP_QUESTION_CHARS` was declared and never used, so a 50k-char question uploaded in full and came back a 400 the user never saw — now blocks submit with a live counter. `hasEnvelopeData` had no caller; the transcript now shows a "Signed" badge (the audit page's existing wording, deliberately not "verified" — the key lookup is server-side only).
    - **Cost-ceiling validation was a red border with no text**, and Save stayed enabled to fail on a toast. Now an inline `role="alert"` wired via `aria-describedby`, and Save is blocked — matching `GroupHitlEditor`.
    - **The phase editor repeated its two help sentences under every phase** (6× for a TASK_FORCE group, in a sidebar). Only visible by rendering it. Stated once in a legend; the disabled control keeps its reason as a tooltip.
    - Attachment chips now show sizes (the total-size cap was otherwise discoverable only by hitting it); duplicates are dropped before the size budget rather than charged to it; and the picked `FileList` is materialized before `input.value` is cleared instead of relying on that racing the async read.
  - **Known gap, tracked separately**: `Workforce.settings.*` holds ~120 strings that were never added to `en.json` and ship in English in every locale. The ~26 added here are translated, which makes the pre-existing gap visible as mixed-language forms.
  - **Tests**: 4675 → 4777. New files for the API surface, `lib/group-config`, the stream events, the phase editor, the config panel and the settings page; the crash fix, the wire-format normalization, the cost-ceiling refusal and the convergence cleanup were each mutation-checked (reverted, watched fail, restored). i18n: 90 keys across all 11 locales, genuinely translated so the `i18n-quality` debt ratchet does not move.
- **Critical Review Hardening** (branch: `claude/repo-review-grading-13c5d7`): Full-repo review followed by fixes for every defect found. Includes:
  - **Chat output corruption**: `formatMarkdownText` applied prose repairs to code, URLs and JSON — its comments claimed to skip link destinations and inline code but the regexes did not. Protected regions are now masked before any rule runs, and the lowercase→uppercase splitter is removed (it cannot distinguish a dropped space from `apiKey`).
  - **SSE framing**: `chat.ts` was the only one of three parsers that assigned rather than appended `data:` lines, trimmed token payloads, and ignored CRLF. Extracted `parseSseFrame` into `sse-utils.ts` as the shared implementation.
  - **409 rollback**: `useSendMessage` tracked its optimistic message id in a per-render `let`, so the documented rollback never ran. Now a ref.
  - **Dead navigation**: four targets resolved to no route and fell through the catch-all to `/welcome` — command-palette agent results, the quick-create redirect, `*view` breadcrumbs, and the mobile Threads tab. Plus two reported bugs: the "Manage Workforce" tile linked to the page hosting it, and the Insights header (with the only back link) rendered only in the success branch.
  - **Error states**: `audit`, `schedules`, `variables`, `logs`, `quotas`, `channel-detail` had no `isError` branch, so a 500 rendered the empty state.
  - **i18n**: Arabic shipped 2 of its 6 required plural categories, so counts of 0/2/3-10/11-99 rendered in English; fr/es/pt lacked `many`; `secrets.emptyHint` was a wrong message in all 10 locales.
  - **Docs/hygiene**: corrected six false claims (React Router version, test count, Dependabot vs Renovate, "all API calls go through ApiClient", supported versions), deduped `.gitignore`, resolved the tracked-and-ignored contradiction for `HANDOFF.md`/`.ds-sync`, removed 5 unused dependencies.
  - **Tests**: 4128 → 4256 (281 → 291 files). Each fix was verified to fail without it by temporarily reverting — including the 409 rollback (red when the ref becomes a per-render `let`), the breadcrumb key collision, the indented-list masking regression, the destructive schedules error state, and the channel-detail refetch guard. Run `npm test` for the current total rather than trusting this number. New permanent guards: `route-integrity.test.ts` (every `to=`/`navigate()` target must resolve off the catch-all) and `i18n-quality.test.ts` (plural completeness, interpolation integrity, untranslated-string ceiling).
  - **React Router v7**: upgraded 6.30.4 → 7.18.2, closing GHSA-wrjc-x8rr-h8h6 (open redirect → XSS) and GHSA-337j-9hxr-rhxg. No source changes needed — the app uses only the declarative subset. 7.18.2's remaining GHSA-qwww-vcr4-c8h2 is RSC-mode only and unreachable here; accepted and documented in `SECURITY.md`, because npm's suggested "fix" downgrades back into the open-redirect range. Added `npm run audit:prod` + a CI step that gates production advisories against an explicit allowlist and fails closed if the audit cannot be read.
  - **Known remaining**: the bundle is a single 8.2 MB chunk with no code splitting; `workforce-wizard` ignores `?template=`; Workforce HITL settings write a config that gates no phase; a paused discussion has no approve/reject control in the Workforce surface; `readGroupSSE` in `lib/api/groups.ts` should migrate to the shared `parseSseFrame` (it trims data payloads and normalises CRLF per chunk); a pre-existing React duplicate-key warning fires on the agent-detail version selector (`key={v.version}`).
- Triggers UI/UX Refactoring: Replaced text inputs with dynamic `AgentPicker` combobox using `useDebounce` and backing API calls. Solved 404 proxy issues inside Vite server.
- Triggers Table Refactoring: Enabled the list filter to filter agent deployments not just by `agentId` but also by resolved `name` leveraging `useAgentDescriptors` and `Map`. 
- Bugfix: Fixed TypeScript build error in `src/pages/triggers.tsx` where `AgentDescriptor` incorrectly accessed an undefined `id` property instead of relying on `parseResourceUri(a.resource).id`.
- Codebase Audit Hardening: Fixed cascade save data corruption issues, standardized URI schemes across resource types, resolved version staleness bugs in the save lifecycle, added secrets API authentication headers, and implemented comprehensive test coverage for all fixes.
- Global Variables Admin Page: New `/manage/variables` route for managing deployment-wide `eddivar` configuration values. API layer, TanStack Query hooks, table UI with search/filter, create/edit/delete dialogs, key validation, export flag, clipboard copy for syntax hints, sidebar nav entry, MSW handlers, i18n (11 locales), 17 unit tests.
- Bugfix: Group CRUD version param — `getGroup()` truthy check omitted required `?version=` param, wizard success link navigated without version, detail page defaulted version to `undefined`. All three caused 400 Bad Request with no backend logs.
- **Agentic Improvements UI** (branch: `feat/capability-registry-ui`): Full UI parity with `feature/agentic-improvements` backend branch. Includes:
  - **LLM Editor**: Behavioral counterweights (level/placement/custom), identity masking (toggle + rules + validation), tool response limits (strategy dropdown + summarizer model + per-tool limits), info banners and contextual warnings.
  - **Agent Config**: Session management (auto-snapshot + trigger chips + max checkpoints), forking (disabled + "coming soon"), cryptographic identity (versioned keys + legacy v0 fallback + expiration), security toggles (disabled with AlertDialog guard).
  - **Rules Editor**: `deploymentContext` and `capabilityMatch` condition types with auto-populated default configs.
  - **Code quality**: Variable shadowing fix, orphaned config cleanup, comprehensive MSW mock data, 12 new tests.
- **Channel Integration Management UI** (branch: `feat/channel-integrations-v2`): Full Slack channel integration management with backend model parity. Includes:
  - **Pages**: `/manage/channels` (listing with card/table views, search, create wizard) and `/manage/channels/:id` (detail editor with inline target management).
  - **Architecture**: Config-driven `ChannelIntegrationConfiguration` model, multi-target routing (Agent/Group types) with trigger keywords, observer mode with configurable guardrails (cooldown, cost, daily limits), vault-enforced credentials via `SecretKeyPicker`.
  - **Components**: `ChannelCard` (interactive card with Slack branding), `CreateChannelDialog` (3-step wizard), `ChannelDetailPage` (full config editor with target cards, observe config, webhook URL, required scopes).
  - **Infrastructure**: API layer (`channels.ts`), TanStack Query hooks, Vite proxy for `/channelstore` + `/integrations`, sidebar entry, i18n (11 locales), MSW handlers (full CRUD + realistic multi-target mock data).
  - **Fixes**: Version bump on save (prevents 409 on double-save), pre-existing `resources.test.tsx` failure fixed (missing MSW `currentversion`/`descriptors` handlers), aria-labels on all remove buttons.
  - **Tests**: 36 new tests (8 API unit, 11 channels page, 17 channel detail page).
- **Code Review Hardening** (branch: `fix/code-review-issues`): Comprehensive code audit and bug fixes. Includes:
  - **SSE Auth**: Replaced native `EventSource` (which cannot send Authorization headers) with `fetch`-based SSE streaming (`sse-utils.ts`). Updated all 6 callers: `logs.ts`, `coordinator.ts`, `live-log-viewer`, `session-log-store`, `use-logs`, `use-coordinator`.
  - **Debugger Error Handling**: Added `isError` handling to `prompt-viewer`, `pipeline-trace`, and `memory-inspector` — they now display error indicators (AlertTriangle) instead of silently showing empty state.
  - **Type Alignment**: Fixed `ViewState` type to match backend enum (`UNSEEN|SEEN` not `UNSEEN|READ|DONE`). Added missing `EXECUTION_INTERRUPTED` to `ConversationState`.
  - **Cost Dashboard**: Fixed API contract mismatches in `ToolCostSummary` and `RestToolHistory` types.
  - **Code Quality**: parseFloat fix (was parseInt), error toast in channel creation, mock data fidelity, constant deduplication.
  - **Tests**: 33 new tests across 3 new test files.
- **Parser Editor** (branch: `feat/parser-editor`): Full parser configuration editor with dual-mode support. Includes:
  - **ParserEditor component**: Form editor for `ai.labs.parser` config — collapsible sections for Configuration (3 toggles), Dictionaries (6 built-in + regular dict CRUD with URI picker), Corrections (3 types with Levenshtein distance), Normalizers (4 types with punctuation config).
  - **Inline workflow editing**: Parser steps in the workflow pipeline show a "Configure" button that opens a dialog for editing parser config without navigating away. Dialog has Escape key handling, body scroll lock, auto-focus, and smooth animations.
  - **Standalone resource support**: Parser registered in `RESOURCE_TYPES` (`parserstore/parsers`), `EDITOR_MAP`, `EXTENSION_TO_SLUG`, and `EXTENSION_TO_RESOURCE_SLUG` — browsable at `/manage/resources/parser/:id`.
  - **Architecture**: Types/constants extracted to `parser-editor-types.ts` (satisfies `react-refresh/only-export-components`). All derived state wrapped in `useMemo`/`useCallback`.
  - **UX polish**: `focus-visible` rings on all interactive elements, URI-based stable React keys, smooth ChevronDown rotation, destructive hover on delete buttons.
  - **i18n**: Full `parserEditor.*` namespace across all 11 locales.
  - **Tests**: 49 new tests (35 parser editor unit, 9 resource detail integration, 5 workflow inline integration). Coverage: 98.82% lines, 89.58% branches.

- **Test Coverage Fixes** (branch: `feat/test-coverage`): Comprehensive testing improvements reaching >70% overall function coverage. Includes:
  - **Interaction Tests**: Expanded test suites for `LogsPage`, `OutputEditor`, `ChannelDetailPage`, and `Onboarding` flow.
  - **Mocks Improvements**: Handled edge cases with environment variables (like `navigator.clipboard` access) and async store state mutations.
  - **Coverage milestone**: Achieved 70.1% total function coverage (from 69.3%).

- **Quotas 404 Fix** (branches: `fix/quotas-404-bootstrap` on EDDI-Manager, `fix/quota-store-bootstrap` on EDDI): Fixed quotas page returning 404 on fresh Mongo/Postgres instances. Includes:
  - **Backend**: Added `@ConfigProperty` injection and default quota bootstrapping to `MongoTenantQuotaStore` (constructor) and `PostgresTenantQuotaStore` (lazy in `ensureSchema()`). Added package-private test constructors. Refactored Postgres `getQuota()` to use connection-reusing `getQuotaInternal()`.
  - **Frontend API**: `getQuota()` and `getUsage()` now catch 404 → return local defaults (no auto-write). Non-404 errors still propagate.
  - **Frontend UI**: Loading skeleton, enforcement-off banner, quota field icons, dimmed fields when disabled, `data-testid` attributes throughout.
  - **Tests**: 30 new frontend tests (19 page + 11 hooks), 4 new backend bootstrap tests. All 860 frontend tests pass, all 112 backend tenancy tests pass.

- **Group Task Orchestration** (branch: `feat/group-task-orchestration`): Full UI support for EDDI's new TASK_FORCE discussion style with live task tracking. Includes:
  - **Types & API**: Added `TASK_FORCE` style, `TaskItem`, `TaskDefinition`, `DynamicAgentConfig`, `SharedTaskList` types. Added `task_plan_created` and `task_verified` SSE event types. Updated `GroupConversation` with `taskList`, `dynamicMembers`, `createdAgentIds`, `retainedAgentIds`. Added `PLAN`, `EXECUTE`, `VERIFY` phase types. Fixed SSE parser default event type safety.
  - **SSE Hook**: Updated `use-group-discussion-stream.ts` with task lifecycle tracking — `taskPlan`, `tasksInProgress`, `tasksCompleted`, `taskVerifications` state fields. Fixed `GroupStartPayload` field name (`groupConversationId`). Speaker events during EXECUTE phase automatically infer task progress. Protected shared initialState from reference sharing bugs.
  - **TaskBoard Component** (NEW): Kanban-style live task board with 4 columns (Pending → Active → Done → Verified). Animated task cards with priority badges (P0–P3), agent avatars, verification feedback. Responsive mobile layout. Progress bar with streaming indicator. CSS pulse animation (moved from inline `<style>` to `index.css` with CSS custom properties). ARIA attributes for accessibility (role=progressbar, region labels). Section heading with icon.
  - **Transcript Integration**: TASK_FORCE theme colors (orange accent), task board mounted during TASK_FORCE discussions (both live SSE and completed conversations loaded from API via `MemoizedApiTaskBoard`). PLAN/TASK_RESULT/VERIFICATION entry types with distinct visual treatments. Fixed flash on stream completion — board persists until API data loads. Fixed `tasksCompleted` inflation (excludes VERIFIED tasks).
  - **Config Panel**: Pre-configured tasks section and dynamic agents configuration display.
  - **Phase Header**: PLAN 📋, EXECUTE ⚡, VERIFY ✅ icons.
  - **Wizard**: TASK_FORCE style option with orange accent in style picker.
  - **i18n**: 27 new keys across `groups.*` and `taskBoard.*` namespaces (incl. `emptyState`, `unassigned`), propagated to all 11 locales.
  - **Code Review**: 10 fixes — TaskBoard flash, ARIA accessibility, inline style → CSS, memoized API path, completed count fix, SSE parser safety, state reset protection, missing i18n keys, CSS custom properties.
  - **Tests**: 62 tests across 4 files — 22 TaskBoard (100% stmt/line/func, 98.24% branch), 17 SSE hook (6 new task-specific), 8 transcript, 15 i18n. Build succeeds.

- **Boardroom App v1** (branch: `feat/advisory-board-wizard`): Standalone advisory board experience within the EDDI Manager. Includes:
  - **Layout**: Responsive shell with collapsible sidebar, mobile bottom tabs, topbar with breadcrumbs. Separate from Manager chrome.
  - **Dashboard**: Board cards with quick actions (settings, history, duplicate, delete), grid/list view toggle (localStorage persisted), welcome hero with template shortcuts for empty state.
  - **Creation Wizard**: 4-step wizard (Name → Team → Style → Review). 3 templates (advisory-board, code-review, pro-con). Team builder with agent search. 5 discussion styles (ROUND_ROBIN, DEBATE, PEER_REVIEW, BRAINSTORM, ARGUMENT). Confetti on success.
  - **Group Chat**: SSE streaming via `useGroupDiscussionStream`, phase-aware transcript with collapsible sections, synthesis footer, file upload support.
  - **1:1 Threads**: Per-agent follow-up conversations from group chat context. File attachments (drag-and-drop + paperclip). localStorage thread registry.
  - **Board Settings**: Team member management (add/remove/reorder), discussion style config, max rounds, danger zone (delete). Form validation, dirty state tracking, `beforeunload` guard.
  - **Conversation History**: Split-view (list + viewer). Search/filter, keyboard arrow navigation, paginated loading, delete with confirmation. Conversation viewer shows full transcript with phase separators.
  - **UX Enhancements**: Copy response (clipboard), pin/bookmark (localStorage), export conversation as markdown, keyboard shortcuts (N/? + dialog), grid/list toggle.
  - **Quality**: 3 full code review rounds. RTL-safe (zero `px-`/`pl-`/`pr-`/`ml-`/`mr-`/`left-`/`right-` violations). Focus traps on all dialogs. `prefers-reduced-motion` for all animations. ARIA landmarks/labels throughout. All `t()` calls have fallbacks.
  - **i18n**: 100+ new keys across `boardroom.*` namespace, propagated to all 11 locales.
  - **Commits**: 8 commits on `feat/advisory-board-wizard` from `4226d6c5` to `13a538ba`.

- **Debug Panel Overhaul + Workforce Rename + Settings Gap Closure** (branch: `test/debug-workforce-coverage`): Comprehensive debug panel redesign, boardroom→workforce rename, and missing configuration coverage. Includes:
  - **Debug Panel Rewrite**: Pipeline waterfall with expandable task detail, cost dashboard with stat cards + per-turn table, memory inspector with key-value search, role-labeled prompt viewer, polished log viewer.
  - **Boardroom → Workforce Rename**: Renamed across 65+ files — directories (`src/pages/workforce/`, `src/components/workforce/`), component prefixes (`Workforce*`), i18n namespaces, route paths, git-tracked directory renames.
  - **CodeRabbit Review Fixes**: Resolved all 10+ critical/major findings — TS type safety (TS2345, TS2532, TS2339), cost dashboard error handling, memory inspector React keys, workforce board panel mutual exclusivity, i18n mojibake repair.
  - **Workforce Settings Config Gaps**: Added 4 new collapsible sections to `workforce-settings.tsx` completing backend parity:
    - **Protocol & Resilience**: Agent timeout, failure policy (Skip/Retry/Abort), max retries, member unavailable policy, max turns.
    - **Human Oversight (HITL)**: Approval granularity (Phase/Task), timeout with ISO-8601 duration, timeout policy (Wait/Auto-approve/Auto-reject/Abort), rejection policy (Fail/Retry).
    - **Dynamic Agents**: Toggle-gated creation/recruitment/delegation with per-discussion limits, lifecycle policy (Ephemeral/Keep/Undeploy/Agent Decides), model inheritance.
    - **Task Definitions**: TASK_FORCE-only pre-configured task list with subject, description, role assignment, priority.
  - **UX Polish**: Chevron disclosure affordance on collapsible sections, hover feedback, module-level defaults for stable React hook refs.
  - **i18n**: 57 new `Workforce.settings.*` keys propagated to all 11 locales.
  - **Tests**: 119 unit tests across debug panel + workforce components, audit test fix (camelCase assertion).
  - **Commits**: 6 commits on `test/debug-workforce-coverage` from `e05391bf` to `feeb5aef`.

- **Platform Operator — Approval Gate + Write Capability** (branch: `feat/operator-write-scope`, stacked on the P1 read-only operator): Takes the operator from read-only to an admin-grantable `read_write` scope, gated end-to-end by a real human-approval workflow rather than a config toggle. Includes:
  - **HITL approval gate (read-only)**: `useVerifyOperatorGate` builds and deploys a throwaway agent config against the backend to confirm the HITL tool-approval gate is actually enforced on this deployment before anything write-capable is ever offered — a version bump alone proves nothing. `gate.verified` is the seam every later write-scope check reads.
  - **Approval surface**: `ApprovalBanner`'s `renderCallExtra` render-prop lets the operator screen show gated-call context inline in the operator chat itself (`operator-chat.tsx`) rather than sending the admin to a separate approvals page — resolving a pause is `requireExplicitPerCall` so a swept-in call can't inherit a top-level Approve unreviewed.
  - **System prompt from the granted endpoint set**: the operator's system prompt is derived from `tool-scopes.ts`'s allow-list rather than hand-maintained prose, so the model is never told about more than it can actually call.
  - **Fixes along the way**: reconciling a resumed turn by placeholder identity (not output-count comparison) and handling a re-pause mid-resume; per-tool `toolApprovals.rules` verification, not just the top-level gate; the pause placeholder id moved from a ref into state (a ref update the resume handler read before React committed it raced the very state it was meant to protect).
  - **`WRITE_ENDPOINTS` populated**: 24 entries, previously an intentionally empty allow-list stub. Four operational verbs (descriptor PATCH, deploy, undeploy, schedule disable), agent and group creation (`setup`, `setup-api`, `POST /groupstore/groups`), the workflow-extension authoring stores (rules/outputs/property-setters/dictionaries/apicalls/mcpcalls/workflows), and `PUT /agentstore/agents/{id}/updateResourceUri` — the hop that makes an edit actually take effect, since EDDI writes version+1 rather than mutating in place. **No `DELETE` and no full `PUT /agentstore/agents/{id}`** — those carry a gate of their own and a whole-document update cannot be checked for weakening without a prior version to diff. `llmstore` writes ARE granted (so the operator can edit prompts and models), but only in combination with `gate-guard.ts`, which hard-refuses any llmstore write carrying `Task.toolApprovals` — the field that FULLY REPLACES the agent-level gate — or any whose body cannot be shown to be free of it. The grant and the guard must not be separated. `tool-scopes.ts` is the source of truth; its doc comment justifies every inclusion and exclusion.
  - **Write canary**: `write-canary.ts` provokes one real gated write and asserts it actually pauses (rejecting the pause so nothing executes) before `read_write` is ever granted; anything but a clean pause rolls the whole activation back (`resetOperator` — undeploy + delete + clear config), since the agent is already deployed with live write tools by that point.
  - **Real scope selection**: `read_write` is now the DEFAULT scope, freely selectable on first activation; read-only is the explicit opt-down. The old two-step bootstrap (activate read-only, reconfigure once the gate was verified) is gone — `isWriteScopeAvailable` only checks that `WRITE_ENDPOINTS` is non-empty, because activation itself proves the gate about the agent it actually creates: `verifyGateInstalled` reads the gate back from the new document, and the write canary refuses to leave a write-capable operator deployed unless a real write provably paused (rollback otherwise).
  - **Backend floor**: operator activation now requires **EDDI 6.2.0+** unconditionally — `READ_ENDPOINTS` includes the docs endpoints (`GET /administration/docs{,/{name}}`, `@since 6.2.0`) and `findMissingEndpoints` hard-refuses activation when the spec lacks an entry. Accepted deliberately over an optional-endpoints tier (which would let the prompt promise docs the agent never got); the error message names the missing paths.
  - **Server-verified request preview**: the backend now resolves and fingerprints the exact HTTP request a gated call will make (EDDI-side: `IApiCallExecutor#resolve`, pinned at gate time, re-checked immediately before execution) and returns a redacted preview through `approval-status`. The operator screen's `RequestPreview` component renders that preview — method/uri/query/headers/body, with a `verified`/`preview` badge depending on whether the backend could pin it — demoting the old client-side `reconstructEndpoint` (`operationId` → guessed path) to a fallback for calls the backend could not resolve ahead of execution.
  - **i18n**: `operator.readWriteChip`, `operator.activation.scope.*`, `operator.stage["write-canary"]`, `operator.toast.activatedReadWrite`, `operator.approval.{verified,verifiedTitle,previewOnly,previewOnlyTitle,query,headers,bodyTruncated}` — all 11 locales.
  - **Backend companion PR** (`labsai/EDDI`, branch `feat/operator-request-fingerprint`): request-fingerprint pinning (`ResolvedRequest`, `RequestRedactor`), `eddi.operator.write.approval{decision}` metric plus the two `/administration/operator/{canary-result,gate-status}` relay endpoints this branch's canary and gate-verification calls report to.
  - **Commits**: 11 commits on `feat/operator-write-scope` from `fa793a04` to `6f47e835`; substantially more has landed on this branch since (request pinning/redaction hardening, self-guard, escalation flags, the approvals inbox, full agent/group create+modify, and the docked drawer below) — see `git log feat/operator-write-scope` rather than trusting this range.
  - **Context-aware side-chat drawer**: the operator was Manager-only and full-page-only — unreachable from Workforce, and always a full navigation away. `operator-drawer.tsx` is a floating launcher mounted once in `AppLayout` and once in each of `WorkforceLayout`'s three viewport branches (self-positioned `fixed`, since those four layouts share no common chrome slot), reusing `useOperatorChat`/`useOperatorConfig` directly — same react-query cache, same conversation, not a second copy. Getting there required promoting `use-operator-chat.ts` off local `useState` onto a Zustand store (`useOperatorChatStore`) so the drawer and the full page render the identical live conversation; the full page's own call sites are unchanged. A pause opens to a compact notice + link to `/manage/operator` rather than a shrunk `ApprovalBanner` — that component is security-reviewed for one full-width surface, not a duplicate. `useCurrentScreenContext()` (route → `{screen, agentId, workflowId, groupId, boardId}` via `matchPath`, since the drawer sits above the routed `<Outlet/>`) threads into `send()` as a `context` argument, which a new unconditional, Qute-conditional section of the system prompt (`BODY_APP_CONTEXT`) reads as `{context.screen}` etc. — existing, previously-unused transport (`InputData.context`), zero backend changes. i18n: `operator.chat.pauseCompact{Fallback,Review}`, `operator.drawer.{title,notActivated,activate}` — all 11 locales.
  - **Still open**: iteration 7 (agent/group authoring UI) and a final whole-branch review have both since landed — nothing tracked as open from this line as of the drawer above; check `git log` for what came after.

### Boardroom v2 Design Decisions

**Decisions confirmed by user:**
| Decision | Answer |
|---|---|
| Design | Dark mode default, EDDI gold/black CI (`--color-primary: #f59e0b`) |
| 1:1 Chat | Streaming, markdown, HITL, file upload, history (no undo/debug) |
| Agent discovery | Show all deployed agents |
| System prompt | Full LLM config editor (Monaco) |
| API keys | BYOK — manage within Boardroom UI, global + per-agent |
| File upload | Both group and individual chats |
| Language switcher | Standalone component in sidebar |

**Phases:**
1. Design system alignment (gold/black CI, replace hardcoded slate/indigo)
2. Sidebar with individual agents section
3. Premium 1:1 agent chat (markdown, streaming, HITL, files)
4. Group chat UX upgrade (markdown rendering, gold theme)
5. Full LLM config editor per agent
6. BYOK API key management
7. Language switcher
8. ~~File upload for group chat~~ — done in `feat/group-collaboration-wave-parity` (inline base64 on the start endpoint; a continuation still cannot carry attachments, by backend design)

**Key files to reference:**
- Design tokens: `src/index.css` (L1-60)
- Manager chat UX: `src/components/chat/chat-panel.tsx`, `chat-message.tsx`, `chat-input.tsx`
- Agent API: `src/lib/api/agents.ts`, `src/hooks/use-agents.ts`
- LLM types: `src/components/editors/llm/types.ts` (systemMessage in `tasks[0].parameters`)
- Secrets API: `src/lib/api/secrets.ts`, `src/hooks/use-secrets.ts`
- Language selector pattern: `src/components/layout/top-bar.tsx` (L188-202)
- Workforce files: `src/pages/workforce/`, `src/components/workforce/`, `src/styles/advisory.css` (the `boardroom/` directories these notes used to name were renamed to `workforce/`)

### Test Counts
- Frontend: run `npm test` for the authoritative count. Hardcoding it here meant
  two different totals in one document; the number rots within a commit or two.
- 112 Backend tenancy tests passing (`mvn test`)

### Last Commit Focus
- Frontend: repo-wide review remediation on `claude/repo-code-review-06ded0`. What it changed, and why each mattered:
  - **`gate-guard.ts` check order.** The `requestPinned` test sat *below* the empty-body early return, so an unpinned llmstore write previewed with no body fell straight through the control and left Approve enabled. Reachable by contract: `PendingToolCallView` documents that "a best-effort preview can exist while this is false", and `ResolvedRequestPreview.body` is optional. Pinned is now checked first, with a regression test over every body shape.
  - **Route-level code splitting.** The app shipped as one 8.5 MB chunk (2.35 MB gzipped) that every user downloaded to see any single screen. `app.tsx` now loads pages through `lazyPage()`, and Monaco moved out of the entry chunk into `src/lib/monaco-setup.ts`. Combined with the locale split below, the entry chunk went 8.89 MB / 2.35 MB gzipped → 1.14 MB / 344 KB gzipped — an 85% cut in what a cold visit pays.
  - **i18n drift, closed and gated.** 349 keys were passed to `t()` and present in no locale file, rendering their English fallback in all eleven languages (the whole Workforce namespace, the Analytics screen). All 456 gaps — including the 34 `variables.*` keys the old parity test explicitly waived — are now translated in all 10 locales, and `npm run i18n:check` fails CI on any of four drift classes. Also fixed: `Workforce.dashboard` was a leaf AND a namespace (i18next can resolve only one), and two keys were called with conflicting English defaults.
  - **SSE reconnect.** Four separate unbounded `setTimeout(connect, 5000)` loops — one of them running from app boot — hammered a refused endpoint every five seconds for the whole session. One shared policy now backs off and gives up, finally using the three `SSE_RECONNECT_*` constants that had been exported and unused. `readGroupSSE` also lost its final event on a stream that closed without a blank line, and mis-split CRLF straddling a chunk boundary.
  - **Query-key staleness.** `["agent-descriptor-direct", id]` was read by two Workforce components and invalidated by nothing, so saving an agent left them stale. Keys now come from `src/lib/query-keys.ts`.
  - **RTL/`lang`.** `RTL_LANGUAGES` claimed `he`/`fa`/`ur` with no bundles behind them, so a Hebrew browser got English text laid out right-to-left under `lang="he-IL"`. Direction and `lang` now follow i18next's *resolved* language.
  - **Locale code-splitting.** `i18n/config.ts` statically imported all 11 locale files, so every user downloaded all of them — 2.7 MB raw / ~710 KB gzipped, which had become larger than the application code itself. Only `en.json` is bundled now (it is `fallbackLng`, so it must resolve synchronously); the other ten arrive through a ~15-line i18next backend, one chunk each. `main.tsx` awaits `i18nReady` before the first render, so a non-English user never sees a flash of English. Entry chunk **997 KB → 344 KB gzipped**.
  - Plus: `e2e/` is type-checked at last, lint covers it at `--max-warnings 0`, dead code and dead CI config removed, and the docs below corrected against the code.
- Earlier frontend: `feat(operator): add the Platform Operator agent (P1, read-only)` on `feat/platform-operator-agent`
  - Opt-in, admin-activated agent that inspects this EDDI deployment through its own REST API, exposed as tools via `setup-api`. Read-only allow-list (`src/lib/operator/tool-scopes.ts`), non-editable safety preamble, single-blob `platform.operator` config, activation flow with a post-provision spec check, operator screen with a live tool-activity trace, dashboard discovery card, kill switch, `operator.*` i18n across 11 locales.
  - **Design correction:** the design assumed EDDI forwards the caller's token to an API agent's tool calls. It did not. That gap is now closed in the backend (labsai/EDDI#613) by a `${caller:token}` resolver, and the operator's `authMode: "caller-identity"` uses it: EDDI substitutes the token while building the request, releasing it only for a same-origin call, only into a header, and never persisting it. `"none"` remains the default and is blocked at activation when OIDC is on, since every tool call would 401.
  - **Review pass:** added the post-deploy canary the design asked for (one probe read; it counts tool calls and detects 401s, since a READY badge proves nothing about whether the generated tools can authenticate), made activation honour setup-api's `deployed`/`deploymentStatus` and reject the `"unknown"` agent-id fallback, added a redeploy-in-place path so re-enabling a paused operator no longer rebuilds it, fixed the activation form's accessibility (not one control had an accessible name), and stopped `{{count}}` triggering i18next pluralization.
  - **Contract bug fixed:** `createApiAgent` sent `name`; the backend requires `agentName` and rejects a blank one, so the wizard's API-agent path was broken. Fixed in the type, the wizard, the MSW mock (now rejects blank, as the backend does) and three tests that had been passing vacuously.
- Previous frontend: `feat: fix live logs — REST seeding, SSE resilience, better empty states` on `fix/group-chat-defensive-rendering` (`e5303c5e`)
  - REST API seeding on first SSE connect (session-log-store.ts), BearerEventSource \r\n handling + 45s inactivity timeout, `seeded` state exposed through use-logs hook, 3-state empty view (loading/no-activity/connecting), 3 new i18n keys across 11 locales, 3 new tests
- Backend: `feat: add SSE heartbeat to log stream endpoint` on `feat/v6.2.0-prep` (`cd0551925`)
  - 15s heartbeat comment events in RestLogAdmin.streamLogs(), AtomicLong lastEventTime tracking, 3 new Mockito tests (idle heartbeat, recent-event suppression, closed-sink stop)
