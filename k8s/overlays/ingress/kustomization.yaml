# Ingress overlay
#
# ⚠️  This is a COMPONENT overlay — it does NOT include the base EDDI manifests.
# You MUST apply it together with a database overlay + edit the host in ingress.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eddi

resources:
  - ingress.yaml
